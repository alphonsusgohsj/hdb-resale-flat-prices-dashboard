# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Data artefacts — the warehouse is *built*, not source. Commit the code that
# builds it, never the 200k-row database file.
data/
*.duckdb
*.duckdb.wal
*.tmp/

# The full fact-table export is a 36MB build artefact, regenerated by the
# pipeline. The small aggregated marts ARE committed (see !exports/mart_*.csv)
# so the repo is self-describing and Tableau can point straight at them.
exports/resale_clean.csv

# OS
.DS_Store
